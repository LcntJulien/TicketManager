namespace TicketManager.Core.Enums;

public enum TicketStatus
{
    Backlog = 0,
    Open = 1,
    InProgress = 2,
    Closed = 3
}
public enum TicketPriority
{
    Low = 0,
    Medium = 1,
    High = 2
}